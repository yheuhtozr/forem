<%= t.v.auth.title.text(
      name: community_name,
      maybe_size: display_estimated_user_count? ? t.v.auth.title.size(number_with_delimiter(estimated_user_count)) : "",
      label: community_members_label,
    ) %>
