<%= t("v.auth.title.text_html",
      name: community_name,
      maybe_size: display_estimated_user_count? ? t("v.auth.title.size", num: number_with_delimiter(estimated_user_count)) : "",
      label: community_members_label,
    ) %>
